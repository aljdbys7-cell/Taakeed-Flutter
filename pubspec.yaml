name: taaked_app
description: "Instant Payment Validation Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_fonts: any
  intl: ^0.20.2

flutter:
  uses-material-design: true
  
